name: Deploy React Client to Azure
on:
  push:
    branches: [ "Sarah" ] # Covers both case possibilities

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install and Build
        working-directory: ./spark2scale_.client
        run: |
          npm install
          npm run build
          
          # Create a fresh deployment folder at the root
          mkdir -p ../deploy_out
          
          # Copy standalone files into the root of deploy_out
          cp -r .next/standalone/. ../deploy_out/
          
          # Manually move static assets to where Next.js expects them
          mkdir -p ../deploy_out/.next/static
          cp -r .next/static/. ../deploy_out/.next/static/
          cp -r public/. ../deploy_out/public/ || true

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'spark2scale-client'
          publish-profile: ${{ secrets.AZURE_CLIENT_PUBLISH_PROFILE }}
          # Deploy from the clean root-level folder
          package: ./deploy_out
